class ModelePlanAffaires {
    constructor(id, nom, domaine) {
        this.id = id;
        this.nom = nom;
        this.domaine = domaine;
    }

    creerModel() {
        // Logique pour créer un modèle
    }

    creerSection() {
        // Logique pour créer une section
    }

    creerContenu() {
        // Logique pour créer du contenu
    }
}
