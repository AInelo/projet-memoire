class ExpertFinancier {
    constructor(id, nom, prenoms, contact) {
        this.id = id;
        this.nom = nom;
        this.prenoms = prenoms;
        this.contact = contact;
    }

    evaluerRentabilite(planAffaires) {
        // Logique pour évaluer la rentabilité
    }

    lireDonneesFinanciere(planAffaires) {
        // Logique pour lire les données financières
    }

    analyseFinanciere(planAffaires) {
        // Logique pour analyser les données financières
    }

    donnerDisponibilite() {
        // Logique pour donner la disponibilité de l'expert
    }
}
